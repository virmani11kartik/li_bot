<launch>
    <let name="urdf_path" 
        value="$(find-pkg-share libot_description)/urdf/libot.urdf.xacro" />
    <let name="gazebo_config_path"
        value="$(find-pkg-share libot_bringup)/config/gazebo_bridge.yaml" />
    <let name="rviz_config_path" 
        value="$(find-pkg-share libot_description)/rviz/urdf_config.rviz" />

    <!-- To launch GZ in a given world -->
    <include file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py">
        <!--<arg name="gz_args" value="empty.sdf -r" />  -r to start the time when gz starts -->
        <arg name="gz_args" value="$(find-pkg-share libot_bringup)/world/test_world.sdf -r" />
    </include>
    <!-- To spawn the robot in GZ from the URDF generated by this robot_description topic -->
    <node pkg="ros_gz_sim" exec="create" args="-topic robot_description" />

    <!-- To launch the URDF Robot State Publisher -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher">
        <param name= "robot_description" 
            value="$(command 'xacro $(var urdf_path)')" />
    </node> 

    <!-- To bridge ros and upload the config file to bridge topic by topic -->
    <node pkg="ros_gz_bridge" exec="parameter_bridge">
        <param name="config_file" value="$(var gazebo_config_path)" />
    </node>

    <!-- Launch Rviz with the configuration -->
    <node pkg="rviz2" exec="rviz2" output="screen"
          args="-d $(var rviz_config_path)" />

</launch>